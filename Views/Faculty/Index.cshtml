@model IEnumerable<Projeto.Models.Faculty>

@{
    ViewData["Title"] = "Index";
}

<h1>Index - Universitário</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>
<table class="table table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Facultycoll)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Facultycourse)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Maddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mno)
            </th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Facultycoll)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Facultycourse)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Maddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mno)
            </td>
            <td class="btn-group" role="group" aria-label="Basic outlined example">
                <a asp-action="Edit" asp-route-id="@item.MemberId" class="btn btn-outline-primary">Edit</a>
                <a asp-action="Details" asp-route-id="@item.MemberId" class="btn btn-outline-info">Details</a>
                <a asp-action="Delete" asp-route-id="@item.MemberId" class="btn btn-outline-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
